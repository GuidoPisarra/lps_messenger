// Función para iniciar un chat con un usuario específico
function startChat(username) {
    document.getElementById('usuario-chat').innerHTML = username;
    document.getElementById('recept').value = username;
}